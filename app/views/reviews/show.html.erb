<% content_for(:title, @review.title.slice(0,15)) %>
<div class="container mx-auto px-8 pt-8 pb-60 flex flex-col items-center w-full max-w-md">
  <div class="card bg-base-100 w-full shadow-xl mb-4 mx-auto">
    <div class="card-body">
      <p><%= l @review.created_at, format: :short %></p>  
      <h2 class="card-title"><%= @review.title %></h2>
      <% if @review.summary.present? %>
        <p><%= @review.summary %></p>
      <% else %>
        <p>レビューがまとめられていません</p>
      <% end %>
    </div>
  </div>
  <div class="w-full max-w-sm md:max-w-md mx-auto">
    <%= render partial: 'conversations/conversation', collection: @conversations %>
  </div>
</div>